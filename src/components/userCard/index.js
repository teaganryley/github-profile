/* eslint-disable */
import React from 'react';
import PropTypes from 'prop-types';

const UserCard = props => {
  return (
    <div>

    </div>
  );
};

UserCard.propTypes = {

};

export default UserCard;
